/**
 * Phase 5 — location switcher + Phase 6 station picker. A small floating panel
 * (top-right corner) that lets the user hop between locations and choose a
 * radio station independently. Phase 0 ships the stub.
 */

export function renderLocationSwitcher(/* root, store */) {
  // Phase 5.
}

export function renderStationPicker(/* root, store */) {
  // Phase 6.
}
