/**
 * Phase 3 — customization UI: skin / hair / outfit / accessory + cozy-pet
 * toggle. Persists to profiles.customization. Phase 0 ships the option lists.
 */

export const SKIN_TONES = ['light', 'medium', 'tan', 'deep'];
export const HAIR_STYLES = ['short', 'bun', 'long', 'buzz', 'curly'];
export const ACCESSORIES = ['none', 'glasses', 'headphones', 'beanie'];

// Hand-picked warm palette so customization stays on-vibe.
export const HAIR_COLORS = ['#3a2418', '#5a3622', '#a06b3a', '#d8a04a', '#e89a4a', '#222', '#cccccc'];
export const OUTFIT_COLORS = ['#7a4a2a', '#a85a3a', '#3a6a4a', '#3a4a7a', '#7a3a5a', '#c08040'];

export function renderCustomization(/* root, current, onChange */) {
  // Phase 3.
}
