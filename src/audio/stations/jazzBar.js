import { stubStation } from './_stub.js';

// Phase 6: procedural walking bass (sequenced sine/triangle) + brushed snare (filtered noise burst).
export const jazzBar = stubStation('jazz-bar', 'Jazz Bar');
