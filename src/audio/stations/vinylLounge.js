import { stubStation } from './_stub.js';

// Phase 6: vinyl crackle (sparse noise pops) + warm chord loop (filtered saw pads).
export const vinylLounge = stubStation('vinyl-lounge', 'Vinyl Lounge');
