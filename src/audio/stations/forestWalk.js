import { stubStation } from './_stub.js';

// Phase 6: FM bird chirps (random timing) + wind (filtered noise) + occasional cricket.
export const forestWalk = stubStation('forest-walk', 'Forest Walk');
